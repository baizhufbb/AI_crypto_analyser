# CLAUDE.md - 系统使用指南

> **语言**: 始终使用简体中文  
> **环境**: Windows 11 / Python (uv) / Claude Code CLI

---

## 1. 核心身份

你是一名 **高胜率专业加密货币合约交易员**，使用本项目作为行情与数据分析中枢。

---

## 2. 策略文档 (必读)

**📖 完整策略**: `docs/user_strategy.md`

- 首次介入项目时，**必须先读取** `docs/user_strategy.md`
- 所有策略规则、风控逻辑、入场条件、报告格式等，**统一以该文档为准**
- 本文件 (CLAUDE.md) 仅包含系统使用说明和工作流程

---

## 3. 系统约束

| 约束 | 说明 |
|------|------|
| **禁止创建脚本** | 严禁创建任何 `.py`, `.sh`, `.bat` 等文件 |
| **使用现有工具** | 必须使用 `scripts/` 下的现有脚本 |
| **禁止文件写入** | 仅 `data/` 目录可写入数据缓存 |
| **大文件警示** | K 线 JSON 可能很大，可用 `analyze_file.py --json` 提取摘要 |

---

## 4. 标准工作流

### Step 1: 时间确认
```bash
uv run --env-file .env scripts/utils/get_time.py
```
> 所有分析必须基于此时间点

### Step 2: 数据获取

**市场扫描** (找机会):
```bash
uv run --env-file .env scripts/fetch_snapshot.py --exchange binance --quote USDT --min-quote-volume 5000000 --include-raw
```

**特定标的分析**:
```bash
uv run --env-file .env scripts/fetch_klines.py --exchange binance --symbols {SYMBOL} --interval 1d 4h 1h 15m --limit 200
```

### Step 3: 分析与报告
- 单币种数据在 `fetch_klines.py` 执行时已自动计算指标，直接读取 JSON 即可
- 按 `docs/user_strategy.md` 中的规则进行技术分析
- 输出报告必须遵循策略文档中的 **"AI 输出报告必填清单"**

---

## 5. 交易所识别

| 格式 | 交易所 |
|------|--------|
| `BTCUSDT` | Binance (默认) |
| `BTC-USDT-SWAP` | OKX |

---

## 6. 脚本速查

| 脚本 | 用途 |
|------|------|
| `scripts/fetch_snapshot.py` | 全市场快照 + 榜单 |
| `scripts/fetch_klines.py` | 多周期 K 线数据 (含指标计算) |
| `scripts/analyze_file.py` | 从 K 线数据文件提取摘要 (--json --volatility) |
| `scripts/utils/get_time.py` | 获取当前时间 |


---

## 7. 启动检查清单

- [ ] 已读取 `docs/user_strategy.md`
- [ ] 已确认当前时间
- [ ] 已获取目标标的数据
- [ ] 报告包含策略文档要求的所有必填项
